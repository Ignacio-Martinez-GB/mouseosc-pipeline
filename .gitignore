# imágenes sueltas en la raíz (temporales; las figuras van en resultados/)
/*.png

# entornos y cachés
.venv/
__pycache__/
*.pyc
.pytest_cache/
pytest-cache-files-*/
*.egg-info/
.DS_Store

# salidas generadas (reproducibles desde los datos + config)
resultados/
resultados_test/
PRUEBA_COMPLETA_*/
config_test.yaml
examples/datos_sinteticos/

# datos crudos (no versionar archivos grandes)
Datos/
*.mat
*.abf
*.nwb

# manifiesto (es específico de cada usuario/dataset; no se versiona)
manifest.csv
manifest_*.csv
!manifest_ejemplo.csv   # excepción: el ejemplo SÍ se versiona (muestra el formato)

# guía local de git (no forma parte del paquete)
GIT_Y_GITHUB.md

.gitprobe
